version: "2.1"

services:
  wifi-connect:
    image: balenablocks/wifi-connect:aarch64
    restart: always
    network_mode: host
    privileged: true
    labels:
      io.balena.features.dbus: "1"
      io.balena.features.firmware: "1"


  pijuice:
    build: ./pijuice
    privileged: true
    labels: 
      io.balena.features.balena-api: '1'
      
  sample:
    build: ./sample

  # balena-cam:
  #   build: ./balena-cam/balena-cam
  #   privileged: true
  #   restart: always
  #   network_mode: "host"
  #   labels:
  #           io.balena.features.kernel-modules: '1'

  timelapse-camera:
    build: ./timelapse-camera
    restart: always
    privileged: true
    labels: 
      io.balena.features.balena-api: '1'
      
